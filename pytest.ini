[pytest]
minversion = 6.0
addopts = -ra -q --strict-markers
testpaths = tests
markers =
    slow: marks tests as slow running
    integration: marks integration tests
    requires_data: marks tests needing test data
